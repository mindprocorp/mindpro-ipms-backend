<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.mindpro.ipms.domain.ai.repository.db3.ModelMapper">
	
    <!-- 단건 조회 (사용자 본인 소유만) -->
    <select id="findByConnectionSeq" resultType="kr.co.mindpro.ipms.domain.ai.vo.AiConnectVo">
        SELECT * FROM "MP_IPMS_PA".utb_connection 
        WHERE connection_seq = #{connectionSeq} 
          AND user_mst_seq = #{userMstSeq} 
          AND del_yn = 'N'
    </select>

    <!-- 사용자별 목록 조회 -->
    <select id="findAllByUserMstSeq" resultType="kr.co.mindpro.ipms.domain.ai.vo.AiConnectVo">
        SELECT * FROM "MP_IPMS_PA".utb_connection 
        WHERE user_mst_seq = #{userMstSeq} 
          AND del_yn = 'N' 
        ORDER BY connection_seq ASC
    </select>
    <insert id="insertModel" useGeneratedKeys="true" keyProperty="connectionSeq" keyColumn="connection_seq">
        INSERT INTO "MP_IPMS_PA".utb_connection (
            user_mst_seq, ai_type, ai_base_url, ai_model_nm, ai_api_key, ai_temperature, create_user, create_at, update_user, update_at, del_yn
        ) VALUES (
            #{userMstSeq}, #{aiType}, #{aiBaseUrl}, #{aiModelNm}, #{aiApiKey}, #{aiTemperature}, #{createUser}, current_timestamp, #{updateUser}, current_timestamp, 'N'
        )
    </insert>

    <update id="updateModel">
        UPDATE "MP_IPMS_PA".utb_connection 
        SET ai_type = #{aiType},
            ai_base_url = #{aiBaseUrl},
            ai_model_nm = #{aiModelNm},
            ai_api_key = #{aiApiKey},
            ai_temperature = #{aiTemperature},
            update_user = #{updateUser},
            update_at = current_timestamp
        WHERE connection_seq = #{connectionSeq} 
          AND user_mst_seq = #{userMstSeq}
    </update>

    <update id="deleteModel">
        UPDATE "MP_IPMS_PA".utb_connection
        SET del_yn = 'Y',
            update_user = #{updateUser},
            update_at = current_timestamp
        WHERE connection_seq = #{connectionSeq} 
          AND user_mst_seq = #{userMstSeq}
    </update>
    
</mapper>